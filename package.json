{
  "name": "qr",
  "version": "1.0.0",
  "description": "Tiny tool that enables QR code generation without affiliation, third party tracking etc. Just generat the QR code you need and it is yours.",
  "source": "src/index.html",
  "scripts": {
    "start": "parcel",
    "build": "parcel build src/index.html  --public-url ./ --dist-dir dist --no-cache",
    "dev": "parcel serve src/index.html  --public-url ./ --dist-dir dist --no-cache"
  },
  "dependencies": {
    "bootstrap": "^5.3.7",
    "zxing-wasm": "^2.2.0",
    "@parcel/transformer-sass": "^2.15.4"
  },
  "devDependencies": {}
}